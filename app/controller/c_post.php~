/*<?php
require_once '../model/m_post.php';

if(isset($_POST) and $_POST['submitForm'] == "post" )
{
$document = array(
		$usr_post => $_POST['usr_post']);
$error = array();

if(count($error) == 0){
$user = new User();
if(!($user->check_existence(array($user_username))
{
	$user->create($document);
}
}
?>*/
